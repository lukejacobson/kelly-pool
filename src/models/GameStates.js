// const enumValue = (name) => Object.freeze({toString: () => name});

const GameStates = {
  ADD_PLAYERS: "GameStates.ADD_PLAYERS",
  PLAY: "GameStates.PLAY",
  GAME_OVER: "GameStates.GAME_OVER",
};

export default GameStates